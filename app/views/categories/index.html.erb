<h1>Categorias</h1>

<table>
  <tr>
    <th>id</th>
    <th>nome</th>
  </tr>
 
<% @categories.each do |category| %>
  <tr>
    <td><%= category.id %></td>
    <td><%= category.name %></td>
  </tr>
<% end %>
</table>